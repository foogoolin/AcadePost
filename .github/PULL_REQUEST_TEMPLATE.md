# What kind of change does this PR introduce?

Example: bug fix, feature, docs update, deploy update.

# Why was this change needed?

Link related issues when possible and explain the user-facing reason for the change.

# Verification

List the commands or smoke checks you ran.

# Checklist

- [ ] The change is scoped to one clear problem.
- [ ] No real secrets, tokens, `.env` files, logs or local agent outputs are included.
- [ ] Docker/deploy changes keep image configuration domain-agnostic.
- [ ] User-facing AcadéPost branding is preserved.
