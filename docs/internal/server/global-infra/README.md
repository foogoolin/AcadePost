# Infrastructure globale serveur locale

Ce dossier remplace l'ancien dossier local `server-docs/`.

Son contenu est ignore par Git sauf ce `README.md`, car il peut decrire le serveur complet, ses services, ses chemins et son infrastructure personnelle. Ces informations ne sont pas le guide public d'installation AcadéPost.
